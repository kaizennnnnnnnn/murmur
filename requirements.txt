faster-whisper>=1.0.3
sounddevice>=0.4.6
numpy>=1.26
scipy>=1.11
pynput>=1.7.7
PySide6>=6.6
pyperclip>=1.8.2

# scipy is not optional: audio.high_pass() runs on every single dictation
# (main.py), and Recorder.stop() needs resample_poly whenever the mic will
# not open at 16 kHz -- which is most Windows devices.

# Pillow is needed only by scripts/export_icon.py, which regenerates
# Murmur.ico from assets/. Not needed to run the app.
